def pickingNumbers(a):
    freq = Counter(a)  
    max_length = 0
    
    for num in freq:
        current_length = freq[num] + freq.get(num + 1, 0)
        max_length = max(max_length, current_length)
    return max_length
