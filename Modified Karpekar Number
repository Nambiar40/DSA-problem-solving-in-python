def kaprekarNumbers(p, q):
    res = []

    for n in range(p, q + 1):
        sq = str(n * n)
        d = len(str(n))

        # Split into left and right parts
        r = int(sq[-d:]) if d <= len(sq) else 0
        l = int(sq[:-d]) if sq[:-d] != "" else 0

        if l + r == n:
            res.append(n)

    if res:
        print(" ".join(map(str, res)))
    else:
        print("INVALID RANGE")
