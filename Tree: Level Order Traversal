def levelOrder(root):
    if root is None:
        return
    
    q = [root]  
    
    while q:
        node = q.pop(0)              
        print(node.info, end=" ")    
        
        if node.left:
            q.append(node.left)
        if node.right:
            q.append(node.right)


