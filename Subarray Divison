def birthday(s, d, m):
    count = 0
    current_sum = sum(s[:m])
    
    if current_sum == d:
        count += 1
    
    for i in range(m, len(s)):
        current_sum += s[i] - s[i-m]  
        if current_sum == d:
            count += 1
    
    return count
